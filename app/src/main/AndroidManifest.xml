<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.collegeastra">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CollegeAstra">
        <activity android:name=".activities.UsnActivity"></activity>
        <activity android:name=".activities.BookIdActivity" />
        <activity android:name=".activities.AddBookActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.AddStudentActivity" />
        <activity android:name=".activities.LibrarianHomeActivity" />
        <activity android:name=".activities.StudentHomeActivity" />
        <activity android:name=".activities.SearchActivity" />
        <activity android:name=".activities.BookDetailsActivity" />
        <activity android:name=".activities.CopyActivity" />
        <activity android:name=".activities.IssueBookActivity" />
        <activity android:name=".activities.RecordActivity" />
    </application>

</manifest>